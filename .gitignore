# Scratch / local
scratch/
*.log
.superpowers/

# calibrate / apply runtime backups (created next to the roster on apply)
*.bak
roster.json.bak

# python bytecode (e.g. when a fixture is run/imported during review)
__pycache__/
*.pyc

# macOS
.DS_Store
**/.DS_Store

# internal docs — kept locally, not published
.serena/
docs/specs/
docs/superpowers/
